A basic recipe site using HTML and some CSS.

It allowed me to practice basic HTML skills with vscode, using a boilerplate, adding headings, paragraphs, images, links, lists, and keeping track with git.